{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://retireaway.com/schemas/criterion.schema.json",
  "title": "Criterion",
  "description": "A rule-based requirement used to score destinations",
  "type": "object",
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "A short, URL-friendly unique identifier for the criterion",
      "examples": ["affordability-high"]
    },
    "rule": {
      "type": "string",
      "description": "The logical data rule",
      "examples": ["destination has affordability A or B"]
    },
    "pro": {
      "type": "string",
      "description": "User-facing message shown when this criterion IS met",
      "examples": ["Fits your budget"]
    },
    "con": {
      "type": "string",
      "description": "User-facing message shown when this criterion is NOT met",
      "examples": ["May exceed budget"]
    }
  },
  "required": ["slug", "rule", "pro", "con"],
  "additionalProperties": false
}
