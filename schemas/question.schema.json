{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://retireaway.com/schemas/question.schema.json",
  "title": "Question",
  "description": "A question used to determine user retirement preferences",
  "type": "object",
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "A short, URL-friendly unique identifier for the question",
      "examples": ["monthly-budget"]
    },
    "title": {
      "type": "string",
      "description": "The primary question text",
      "examples": ["What is your estimated monthly budget?"]
    },
    "description": {
      "type": "string",
      "description": "Additional context or clarification for the question",
      "examples": ["This should include housing, healthcare, and groceries."]
    },
    "answers": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "description": "A list of answer slugs associated with this question",
      "examples": [["budget-under-2k", "budget-mid-range"]]
    }
  },
  "required": ["slug", "title", "description", "answers"],
  "additionalProperties": false
}
